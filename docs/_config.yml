title: Walmart Cashback Pro
baseurl: "/walmart-cashback-pro"
url: "https://gategith.github.io"
include: [".nojekyll"]

plugins:
  - jekyll-seo-tag

defaults:
  - scope:
      path: ""
    values:
      layout: default

security:
  headers:
    Content-Security-Policy: |
      default-src 'self' https://cpagrip.com;
      script-src 'unsafe-inline' 'self';
      style-src 'unsafe-inline' 'self';
